# Python dependencies for rsona vs librosa benchmark suite
# Install with: pip install -r requirements.txt

# Core audio processing
librosa>=0.10.0,<0.11.0
numpy>=1.20.0,<2.0.0
scipy>=1.7.0,<2.0.0

# Visualization
matplotlib>=3.5.0,<4.0.0

# Optional: for better performance with librosa
numba>=0.56.0,<0.59.0

# Optional: for audio file handling
soundfile>=0.12.0
audioread>=3.0.0

# Optional: for prettier JSON output during development
# (not required for benchmark execution)
# jq  # Install separately via system package manager
